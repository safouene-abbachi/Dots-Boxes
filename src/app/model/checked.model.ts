export class CheckedModel {

    public id?: string;
            idchecked: string;
            player: string;
          
 
     constructor( id: string,
        idchecked: string,
                  player: string,
                 ) 
               {
 
                this.id = id;
                this.idchecked = idchecked;
                this.player = player;
                
              }
 }
 